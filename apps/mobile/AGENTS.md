# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Also read the repo convention at ../../docs/conventions/11-mobile-expo.md. It is canonical for
Ultron mobile architecture, design tokens, feature-folder layering, and the rule that mobile is only
an input/output adapter for the shared agent runtime.
